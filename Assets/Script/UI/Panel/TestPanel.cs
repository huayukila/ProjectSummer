using UnityEngine;

public partial class TestPanel : ViewController
{
    public Sprite sprite;
    // Start is called before the first frame update
    void Start()
    {
        BG.sprite = sprite;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
